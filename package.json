{
  "name": "tze",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm -F viewer dev",
    "build": "pnpm -F @ucam-eo/maplibre-zarr-tessera build && pnpm -F viewer build",
    "test": "pnpm -r test",
    "check": "pnpm -r check"
  },
  "engines": {
    "node": ">=20"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"],
    "overrides": {
      "zarrita": "link:../zarrita.js/packages/zarrita",
      "@zarrita/storage": "link:../zarrita.js/packages/@zarrita-storage"
    }
  }
}
