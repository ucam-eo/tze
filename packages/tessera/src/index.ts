// @ucam-eo/tessera — core TESSERA embedding access library

export type {
  TesseraOptions,
  StoreMetadata,
  ChunkRef,
  ManagedChunk,
  EmbeddingRegion,
  EmbeddingAt,
  ZoneDescriptor,
  EmbeddingProgress,
  DebugLogEntry,
  UtmBounds,
  TesseraEvents,
  TileRendererOptions,
} from './types.js';
